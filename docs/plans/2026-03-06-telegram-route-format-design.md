# Telegram Route Formatting Design

- Goal: mirror `/app` trip-plan detail density in Telegram fallback messages.
- Format: 2-3 option blocks; each block shows mode, duration, leg lines, and transfer wait lines.
- Source of truth: same trip planner engine used by `/app`.
